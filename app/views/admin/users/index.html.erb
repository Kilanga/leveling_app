<h1>Gestion des utilisateurs</h1>

<table class="table">
  <thead>
    <tr>
      <th>Pseudo</th>
      <th>XP</th>
      <th>Orbes</th>
      <th>Fragments</th>
      <th>Admin</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.pseudo %></td>
        <td><%= user.xp %></td>
        <td><%= user.coins %></td>
        <td><%= user.respond_to?(:free_credits_balance) ? user.free_credits_balance : 0 %></td>
        <td><%= user.admin? ? "✅" : "❌" %></td>
        <td>
          <%= link_to "Modifier", edit_admin_user_path(user), class: "btn btn-warning" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
