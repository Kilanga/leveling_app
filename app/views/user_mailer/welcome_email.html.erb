<h1>Bienvenue, <%= @user.email %> ! 🎉</h1>
<p>Merci de nous avoir rejoints. Nous sommes ravis de vous avoir parmi nous.</p>
<p>Cliquez sur le lien ci-dessous pour accéder à votre compte :</p>
<a href="<%= ENV.fetch('APP_HOST', 'localhost:3000').start_with?('http') ? ENV.fetch('APP_HOST', 'localhost:3000') : "https://#{ENV.fetch('APP_HOST', 'localhost:3000')}" %>">Accéder à l'application</a>
